/**
 * Set 'production' to 'true', to target the defaultAPI and retrieve data from the API.
 * Set it to 'false' to work with mock data.
 */

export const Environment = {
  defaultAPI: 'https://api.mocki.io/v2/016d11e8',
  production: true
}
